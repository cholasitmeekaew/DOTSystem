# โหมดปัจจุบัน: ใช้ฐานข้อมูลจำลอง (JSON/localStorage) — ไม่ต้องตั้งค่าอะไร
#
# === โหมด Supabase (ฐานข้อมูลจริง) ===
# ตั้งค่าด้านล่าง → restart dev server
#
# VITE_SUPABASE_URL=https://your-project.supabase.co
# VITE_SUPABASE_ANON_KEY=your-anon-key
